{
  "name": "@cloudforge/pulumi",
  "version": "0.1.0",
  "private": true,
  "description": "CloudForge Pulumi engine — implements the InfrastructureEngine port via the Pulumi Automation API.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@cloudforge/core": "workspace:*",
    "@cloudforge/shared": "workspace:*",
    "@pulumi/pulumi": "^3.136.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
